# Циан → Excel — расширение для браузера

Максимум автоматизма без терминала и без консоли: **поставить один раз**, дальше —
**одна кнопка** на странице ЖК. Подходит для Chrome, Edge, Яндекс.Браузера, Brave
(на движке Chromium).

## Установка (один раз, ~1 минута, без терминала)

1. Скачайте папку с расширением:
   на GitHub нажмите **Code → Download ZIP**, распакуйте. Нужна папка `extension`
   (в ней `manifest.json`, `content.js`, `icon.png`).
2. Откройте в браузере страницу расширений:
   - Chrome: `chrome://extensions`
   - Edge: `edge://extensions`
   - Яндекс/Brave: аналогично в меню «Расширения».
3. Включите **«Режим разработчика»** (переключатель справа сверху).
4. Нажмите **«Загрузить распакованное расширение»** и выберите папку `extension`.
5. Готово — расширение установлено.

## Как пользоваться (каждый раз — один клик)

1. Откройте на **cian.ru** страницу нужного ЖК (если просит — войдите в аккаунт и
   пройдите капчу; это и есть «вход как человек», который проходит антибот).
2. Внизу справа появится кнопка **«📊 Выгрузить в Excel»**.
3. Нажмите её. Кнопка покажет прогресс («Собрано 56/139…»), затем браузер **сам
   скачает** файл `cian_<жк>_<дата>.xls`.
4. Откройте в Excel: листы **Сводка** + по комнатности + **Все_лоты**, сортировка
   по ₽/м², кликабельные ссылки «Циан →», срок экспозиции.

## Важно: где нажимать

Кнопку жмите на **основной странице ЖК со списком квартир** на `www.cian.ru`
(раздел «Квартиры»), например:
`https://www.cian.ru/kupit-kvartiru-zhiloy-kompleks-<имя>-<ID>/`

Расширение берёт **точный фильтр по ЖК из самого запроса страницы** (не угадывает),
поэтому регион/город и сам ЖК определяются автоматически — настраивать ничего не
нужно. Если открыть промо-сайт застройщика (`zhk-*.cian.ru`) или страницу без
списка квартир — выгрузка сообщит, что нужно открыть страницу со списком.

Встроена защита: если фильтр по ЖК не применился (Циан вернул бы всю выдачу),
расширение НЕ создаёт файл, а предупреждает — чтобы в таблицу не попали чужие лоты.

## Почему так, а не «всё само без меня»

Циан намеренно отдаёт данные **только живой сессии в реальном браузере** (антибот,
капча, бан серверных IP). Поэтому полностью без вашего браузера получить данные
нельзя — это защита Циан, а не ограничение расширения. Расширение сводит вашу
работу к одному клику на странице, которую вы и так открыли.
