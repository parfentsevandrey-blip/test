"""Константы оформления, снятые с эталонного отчёта (PDF-шаблон).

Все величины извлечены из исходного документа: цвета — из заливок и текстовых
спанов, геометрия — из координат объектов на странице A4 (595.32 x 841.92 pt).
Менять оформление отчёта следует здесь, а не в коде рендера.
"""

# --- Цвета (RGB hex, как в шаблоне) ---------------------------------------
NAVY = "1A3A5C"          # заголовки, подписи в таблице
GOLD = "C9A961"          # линейки, маркеры списков, нумерация на обложке
BODY = "2C2C2C"          # основной текст
MUTED = "5A6E80"         # подзаголовки, курсив
TABLE_LABEL_BG = "FAFAFA"  # заливка левой колонки таблицы характеристик
TABLE_BORDER = "D8D8D8"    # горизонтальные линии таблицы
CALLOUT_BG = "F5F1E8"      # заливка врезки «О городе и районе»
CARD_BG = "FFFFFF"         # фон обычной тематической карточки
CARD_BORDER = "DDE3EA"     # тонкая рамка карточки
CARD_RISK_BG = "F4F7FA"    # фон карточки «Ключевые риски и проверки»
TABLE_GROUP_BG = "EFF2F5"  # строка-подзаголовок в таблице характеристик
KPI_TILE_BG = "F4F6F8"     # плитки KPI — светлый нейтральный фон
BADGE_BG = "F7EFD9"        # плашки статуса у заголовка — светло-золотой фон
PAGE_BG = "FFFFFF"         # цвет страницы: им «рисуется» зазор между плитками

# --- Единицы ---------------------------------------------------------------
PT_PER_CM = 72.0 / 2.54  # 28.3465 pt в сантиметре

# --- Страница (см) ---------------------------------------------------------
PAGE_WIDTH_CM = 21.0
PAGE_HEIGHT_CM = 29.7
PAGE_MARGIN_TOP = 1.90
PAGE_MARGIN_BOTTOM = 2.00
PAGE_MARGIN_LEFT = 2.07
PAGE_MARGIN_RIGHT = 2.07

# Текстовая колонка. Считается из полей, а не задаётся числом: любая правка
# полей должна автоматически двигать таблицы, колонтитулы и снимки.
CONTENT_WIDTH_CM = PAGE_WIDTH_CM - PAGE_MARGIN_LEFT - PAGE_MARGIN_RIGHT    # 16.86
CONTENT_HEIGHT_CM = PAGE_HEIGHT_CM - PAGE_MARGIN_TOP - PAGE_MARGIN_BOTTOM  # 25.80

# --- Размеры шрифта (pt) ---------------------------------------------------
FS_COVER_TITLE = 22
FS_COVER_SUBTITLE = 14
FS_COVER_INTRO = 11
FS_COVER_ITEM = 12
FS_OBJECT_TITLE = 16
FS_OBJECT_SUBTITLE = 10
FS_SECTION = 11
FS_CARD_TITLE = 10.5     # заголовок внутри тематической карточки
FS_TABLE = 9.5
FS_BODY = 10
FS_BULLET_MARK = 11
FS_TABLE_GROUP = 8.5     # подзаголовок группы строк в таблице характеристик
FS_KPI_LABEL = 8         # подпись плитки KPI
FS_KPI_VALUE = 13        # значение плитки KPI
FS_BADGE = 8             # текст плашки статуса
FS_CAPTION = 8.5         # подпись под фотографией, примечание сводной страницы
FS_SUMMARY = 9           # сводная таблица сравнения объектов
FS_HDRFTR = 8.5          # верхний и нижний колонтитулы
FS_CLOSING_TITLE = 10    # подзаголовок блока на закрывающей странице
FS_CLOSING_ITEM = 9.5    # пункт списка на закрывающей странице

FONT = "Calibri"  # в Linux подменяется метрически совместимым Carlito

# Файлы шрифта для измерения ширины строки через PIL. Порядок — по убыванию
# точности: сначала то, чем документ реально набирается в Linux (Carlito),
# затем оригинальный Calibri, затем метрически чужой, но всегда доступный
# DejaVu. Если ни один не найден, ширина оценивается по BADGE_CHAR_W.
FONT_FILES_REGULAR = ("Carlito-Regular.ttf", "calibri.ttf", "DejaVuSans.ttf")
FONT_FILES_BOLD = ("Carlito-Bold.ttf", "calibrib.ttf", "DejaVuSans-Bold.ttf")

# --- Геометрия (pt) --------------------------------------------------------
CONTENT_WIDTH = CONTENT_WIDTH_CM * PT_PER_CM  # 477.92 — ширина текстового блока
TABLE_WIDTH = 451.4      # ширина таблицы характеристик
TABLE_COL_LABEL = 145.0  # ширина колонки с подписью
TABLE_COL_VALUE = 306.4
CALLOUT_WIDTH = 451.9
CARD_WIDTH = CONTENT_WIDTH          # карточка занимает всю ширину текстового блока
CARD_PAD_X = 12.0                   # внутренние поля карточки, pt
CARD_PAD_Y = 8.0
CARD_GAP = 6.0                      # зазор между карточками, pt
CARD_INNER_WIDTH = CARD_WIDTH - 2 * CARD_PAD_X  # доступная ширина внутри карточки

# Варианты карточек: цвет фона, тонкой рамки и акцентной полосы слева
CARD_VARIANTS = {
    "plain": {"bg": CARD_BG, "border": CARD_BORDER, "accent": GOLD},
    "city": {"bg": CALLOUT_BG, "border": GOLD, "accent": GOLD},
    "risk": {"bg": CARD_RISK_BG, "border": CARD_BORDER, "accent": NAVY},
}
BULLET_INDENT = 24.0     # отступ текста маркированного списка
BULLET_HANGING = 12.0
BULLET_GAP = 2.5         # отбивка между пунктами списка, pt    # выступ маркера
SUMMARY_COL_LABEL = 110.0  # колонка показателя в сводной таблице

# --- Плашки статуса --------------------------------------------------------
BADGE_GAP = 6.0          # зазор между плашками статуса
BADGE_PAD_X = 7.0        # горизонтальное поле внутри плашки, с каждой стороны
BADGE_PAD_Y = 3.0        # вертикальное поле внутри плашки
BADGE_SAFETY = 1.5       # запас ширины, чтобы строка не переносилась
BADGE_CHAR_W = 4.2       # запасная оценка ширины символа 8 pt, если шрифт не найден

# Толщина линеек в 1/8 pt (единицы w:sz в OOXML)
RULE_GOLD_SZ = 16        # ~2.0 pt — линейки на обложке и над объектом
RULE_NAVY_SZ = 8         # ~1.0 pt — подчёркивание заголовков разделов
BORDER_HAIRLINE_SZ = 2   # ~0.25 pt — линии таблицы
BORDER_CALLOUT_SZ = 4    # ~0.5 pt — рамка врезки
BORDER_CARD_SZ = 4       # ~0.5 pt — рамка карточки
BORDER_CARD_ACCENT_SZ = 18  # ~2.25 pt — акцентная полоса слева
KPI_GAP_SZ = 12          # ~1.5 pt — зазор между плитками KPI цветом страницы

# Разрядка заголовков (в 1/20 pt, единицы w:spacing)
TRACKING_COVER_TITLE = 70
TRACKING_SECTION = 24
TRACKING_TABLE_GROUP = 20

# сколько первых строк группы держатся за её подзаголовком в таблице характеристик
TABLE_GROUP_KEEP_ROWS = 2
TRACKING_KPI_LABEL = 20

# --- Фотоблок --------------------------------------------------------------
# LibreOffice не подпускает встроенную картинку к полю ближе чем на 0.32 см:
# как только снимок шире (CONTENT_WIDTH_CM − 2 × PICTURE_SAFE_INSET_CM), он
# перестаёт центрироваться, прижимается к этой границе слева и вылезает за
# правое поле. Замерено на LibreOffice: левый край любого широкого снимка
# вставал в 9.07 pt от левого поля независимо от величины полей.
PICTURE_SAFE_INSET_CM = 0.32
PICTURE_SAFE_SLACK_CM = 0.08  # запас на округление размера в EMU
PHOTO_WIDTH_CM = round(
    CONTENT_WIDTH_CM - 2 * PICTURE_SAFE_INSET_CM - PICTURE_SAFE_SLACK_CM, 2
)  # 16.14 — предельная ширина, при которой снимок ещё центрируется
PHOTO_MAX_HEIGHT_CM = 11.60
PHOTO_MIN_HEIGHT_CM = 5.00     # ниже не опускаемся, даже если снимков много
HERO_MAX_HEIGHT_CM = 7.20      # фото фасада под заголовком объекта
PHOTOS_PER_PAGE = 2
PHOTO_GAP_PT = 14.0            # отбивка между снимками на одной странице
PHOTO_CAPTION_BLOCK_CM = 0.60  # подпись под снимком: строка 8.5 pt плюс отбивка
PHOTO_PAGE_SLACK_CM = 1.20     # запас на подписи, разъехавшиеся на две строки

# --- Врезка ----------------------------------------------------------------
# Маркер врезки берётся из основного шрифта: эмодзи 📍 в Calibri/Carlito нет,
# и на чужой машине он подтягивался посторонним шрифтом. U+25AA есть во всех
# начертаниях Carlito и в Calibri.
CALLOUT_MARKER = "▪"

# --- Заголовки служебных страниц -------------------------------------------
SUMMARY_HEADING = "Сравнение объектов"
CLOSING_HEADING = "Итоги и следующие шаги"

# --- Колонтитулы (см) ------------------------------------------------------
HEADER_DISTANCE = 1.20   # от верхнего края листа до верхнего колонтитула
FOOTER_DISTANCE = 1.20   # от нижнего края листа до нижнего колонтитула
