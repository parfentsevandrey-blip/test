# Quire keeps no reflective surface of its own; the defaults cover everything.
# Widget providers, services and activities are entry points declared in the
# manifest and are kept automatically by AGP's generated rules.

-dontwarn org.jetbrains.annotations.**
