<?xml version="1.0" encoding="utf-8"?>
<!--
  One entry: its calendar's colour as a short bar, when it starts, what it is. The bar is the
  same "colour as data" the month card's dots carry — it is never painted from the palette,
  because which calendar an entry belongs to is a fact, not a theme.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/row_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:baselineAligned="false"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/row_chip"
        android:layout_width="3dp"
        android:layout_height="14dp"
        android:layout_marginEnd="8dp"
        android:importantForAccessibility="no"
        android:scaleType="fitXY"
        android:src="@drawable/agenda_chip" />

    <TextView
        android:id="@+id/row_time"
        android:layout_width="52dp"
        android:layout_height="wrap_content"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/row_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:layout_weight="1"
        android:ellipsize="end"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:textSize="12sp" />
</LinearLayout>
