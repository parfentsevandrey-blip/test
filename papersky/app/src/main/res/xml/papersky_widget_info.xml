<?xml version="1.0" encoding="utf-8"?>
<!--
  Resizable from a single cell to the whole screen; every size gets its own layout (SizeMode.Exact).
  No updatePeriodMillis: WorkManager, sunrise/sunset jobs and system events keep it current.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:configure="app.papersky.weather.widget.config.WidgetConfigActivity"
    android:description="@string/widget_description"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:maxResizeHeight="1600dp"
    android:maxResizeWidth="1600dp"
    android:minHeight="110dp"
    android:minResizeHeight="40dp"
    android:minResizeWidth="40dp"
    android:minWidth="250dp"
    android:previewLayout="@layout/widget_preview"
    android:resizeMode="horizontal|vertical"
    android:targetCellHeight="2"
    android:targetCellWidth="4"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen|keyguard"
    android:widgetFeatures="reconfigurable|configuration_optional" />
