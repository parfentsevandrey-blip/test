# Зависимости рендера отчёта по недвижимости Нидерландов.
# Версии закреплены под проверенное окружение (воспроизводимый рендер
# Word и графиков). Python >= 3.10 (разработка/прогон на 3.11).
#
# Установка:  make install      (или: python3 -m pip install -r requirements.txt)

python-docx==1.2.0    # генерация .docx (generate_report.py)
matplotlib==3.11.0    # графики/KPI-диаграммы (charts.py); без него Word
                      # собирается без графиков — см. README, раздел «Ограничения»
