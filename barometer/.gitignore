# Локальная БД с собранными данными (создаётся автоматически)
data/*.db
data/*.db-wal
data/*.db-shm

# Сгенерированный статичный снимок дашборда (содержит данные на момент сборки)
barometer-preview.html

# Python
__pycache__/
*.pyc
.venv/
venv/
.env
