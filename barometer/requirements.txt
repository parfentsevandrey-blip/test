# Барометр мобилизации — зависимости
flask>=3.0
requests>=2.30

# Опционально: глубокий анализ через Claude (включается при ANTHROPIC_API_KEY).
# Можно не ставить — приложение работает и без LLM (правиловый скоринг).
anthropic>=0.40

# RSS парсится стандартной библиотекой (xml.etree) — feedparser НЕ требуется.
