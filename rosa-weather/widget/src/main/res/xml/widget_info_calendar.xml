<?xml version="1.0" encoding="utf-8"?>
<!--
    The month over a painting of its season. Resizable from a date tile to a wall calendar; it
    redraws at midnight (CalendarAlarm) and with the weather, so updatePeriodMillis is 0.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:configure="app.rosa.weather.widget.studio.WidgetStudioActivity"
    android:description="@string/widget_calendar_description"
    android:initialLayout="@layout/widget_loading"
    android:minWidth="250dp"
    android:minHeight="180dp"
    android:minResizeWidth="40dp"
    android:minResizeHeight="40dp"
    android:previewImage="@drawable/widget_preview_calendar"
    android:resizeMode="horizontal|vertical"
    android:targetCellWidth="4"
    android:targetCellHeight="3"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen|keyguard"
    android:widgetFeatures="reconfigurable|configuration_optional" />
